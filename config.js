export const DB_URL = "";

import { initializeApp } from "";
import { 
    getDatabase, 
    ref, 
    push, 
    onValue,
    remove, 
    update 
} from "";

export { initializeApp, getDatabase, ref, push, onValue, remove, update };